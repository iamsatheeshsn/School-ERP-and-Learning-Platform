rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Server-side Admin SDK bypasses rules. Client direct access denied by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
